# Troubleshooting

- Ensure `PROMPTLAYER_API_KEY` is set.
- Ensure `jq`, `curl`, `uuidgen`, and `python3` are installed.
- Check `~/.claude/state/promptlayer_hook.log` for hook errors.
- Verify endpoint reachability for `PROMPTLAYER_OTLP_ENDPOINT`.
